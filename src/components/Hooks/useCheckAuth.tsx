import React, { FunctionComponent, useEffect } from "react";

// interface Props {}

// const useCheckAuth: FunctionComponent<Props> = ({}) => {
//     useEffect(() => {
//         status === "loading"
//             ? setIsLoading(true)
//             : status === "authenticated"
//             ? processAuth()
//             : router.push("/auth/signin");
//     });
//     return r
// };

// export default useCheckAuth;
